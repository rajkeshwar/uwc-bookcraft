{
  "name": "bookcraft",
  "version": "1.0.0",
  "description": "BookCraft — Markdown Book Studio with server-side EPUB export",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "epub-gen-memory": "^1.1.2",
    "@sparticuz/chromium": "^123.0.0",
    "puppeteer-core": "^22.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
